async function load(){if(!date.value)return;slots.innerHTML='Cargando...';let d=await (await fetch(`/api/availability?doctor_id=${doctor.value}&service_id=${service.value}&date=${date.value}`)).json();slots.innerHTML='';d.slots.forEach(s=>{let b=document.createElement('button');b.type='button';b.textContent=s.label;b.onclick=()=>{document.querySelectorAll('.slots button').forEach(x=>x.classList.remove('on'));b.classList.add('on');starts.value=s.value};slots.appendChild(b)})}date.onchange=doctor.onchange=service.onchange=load;